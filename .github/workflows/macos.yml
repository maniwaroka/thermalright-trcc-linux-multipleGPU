name: macOS Installer

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch: {}

permissions:
  contents: write

jobs:
  build-macos:
    name: Build macOS app
    runs-on: macos-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Set up Python 3.12
        uses: actions/setup-python@v6
        with:
          python-version: '3.12'
          cache: 'pip'

      - name: Install system dependencies
        run: |
          brew install libusb create-dmg

      - name: Install Python dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pyinstaller
          pip install ".[nvidia]"

      - name: Extract version
        id: version
        run: |
          ver="${GITHUB_REF_NAME#v}"
          echo "VERSION=$ver" >> "$GITHUB_OUTPUT"

      - name: Build app with PyInstaller
        run: |
          pyinstaller \
            --name "TRCC" \
            --onedir \
            --windowed \
            --icon src/trcc/assets/icons/app.icns \
            --add-data "src/trcc/assets:trcc/assets" \
            --hidden-import PySide6.QtSvg \
            --hidden-import pynvml \
            --collect-submodules trcc \
            --osx-bundle-identifier com.thermalright.trcc \
            --noconfirm \
            src/trcc/__main__.py

          # Add CLI symlink inside the app bundle
          mkdir -p dist/TRCC.app/Contents/MacOS
          ln -sf TRCC dist/TRCC.app/Contents/MacOS/trcc

      - name: Create DMG
        run: |
          create-dmg \
            --volname "TRCC ${{ steps.version.outputs.VERSION }}" \
            --window-pos 200 120 \
            --window-size 600 400 \
            --icon-size 100 \
            --icon "TRCC.app" 150 185 \
            --app-drop-link 450 185 \
            --no-internet-enable \
            "trcc-${{ steps.version.outputs.VERSION }}-macos.dmg" \
            dist/TRCC.app

      - name: Upload DMG artifact
        uses: actions/upload-artifact@v4
        with:
          name: trcc-macos-dmg
          path: trcc-${{ steps.version.outputs.VERSION }}-macos.dmg

      - name: Attach to GitHub Release
        if: startsWith(github.ref, 'refs/tags/v')
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh release upload "$GITHUB_REF_NAME" \
            "trcc-${{ steps.version.outputs.VERSION }}-macos.dmg" \
            --clobber
