module trcc_usb 1.0;

require {
    type unconfined_t;
    type usb_device_t;
    class chr_file { getattr ioctl open read write };
}

allow unconfined_t usb_device_t:chr_file { getattr ioctl open read write };
